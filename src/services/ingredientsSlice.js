import { createSlice } from '@reduxjs/toolkit';

const initialState = {
    ingredients: []
}

export const ingredientsSlice = createSlice({
    name: 'ingredients',
    initialState,
    reducers: {

    }
});